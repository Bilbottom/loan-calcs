[build-system]
requires = [
    "setuptools >= 42.0",
    "wheel",
]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
addopts = "--cov=loan_calcs --cov-report=html --cov-fail-under=50"
pythonpath = [
  "."
]
testpaths = [
    "tests",
]
